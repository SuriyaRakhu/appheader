/**
*
* Navigation functional component.
* Functional Components - https://facebook.github.io/react/docs/components-and-props.html
*
*/

import React from 'react';

import './AppHeader.css';
import {Link} from "react-router";
import {Navbar, Row, Col, Button, Header, Brand,ListGroup, ListGroupItem} from 'react-bootstrap';
import Icon from 'react-icons-kit';
import { horizontalCenter } from 'react-icons-kit';
//import AppBar from '../AppBar';
//import SlideMenu from '../SlideMenu';
const MySpan = 
        horizontalCenter(
            props => <span>{props.children}</span>
    );
    
export class Navigation extends React.Component{
  constructor(){
    super()
    this.state = {toggle: true, name: 'Home'}
  }

  togglemode = () => {
    this.setState({toggle: !this.state.toggle})
  }

  navigateTo = (name) => {
    console.log(name)
    this.setState({toggle: !this.state.toggle, name: name})
  }
  
  render(){

    return(
      <div>
        <Navbar className="customNav" fixedTop={true}>
          <Row>
            <Col xs={3} lg={3} md={3}>
              <div>
                MST Icon
              </div>
            </Col>
            <Col lg={7} xs={7} md={7}>
              <Navbar.Header className="navHead">
                <Navbar.Brand>
                  <a className="heading">Select Territories</a>
                </Navbar.Brand>
              </Navbar.Header>
            </Col>
            <Col xs={2} lg={2} xs={2}>
              <Button className="hamburger" onClick={this.togglemode.bind(this)}>
                <div className="bar1 "></div>
                <div className="bar2"></div>
                <div className="bar3"></div>
              </Button>
            </Col>
          </Row>
        </Navbar>
        <div className="mySidenav">
          <div className="nameHead">
            <p>Hi, <b>Rahul</b></p>
          </div>
          <ListGroup className="noWrap">
           
          </ListGroup>
        </div>
      </div>
    );
  }
}

export default Navigation;
